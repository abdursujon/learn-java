package ArrayList;

/**
 *   1. list.add(val) — add element at end
 *   2. list.add(index, val) — insert at specific index
 *   3. list.addAll(collection) — add all elements from another collection
 *   4. list.get(index) — get element at index
 *   5. list.set(index, val) — replace element at index
 *   6. list.remove(index) — remove by index
 *   7. list.remove(Object) — remove first occurrence of value
 *   8. list.removeIf(lambda) — remove elements matching condition
 *   9. list.clear() — remove all elements
 *   10. list.size() — number of elements
 *   11. list.isEmpty() — check if empty
 *   12. list.contains(val) — check if element exists
 *   13. list.indexOf(val) — first index of value (-1 if not found)
 *   14. list.lastIndexOf(val) — last index of value
 *   15. list.sort(comparator) — sort with custom logic
 *   16. list.forEach(lambda) — loop through elements
 *   17. list.toArray() — convert to array
 *   18. list.subList(from, to) — get a portion of the list
 *   19. list.replaceAll(lambda) — transform every element
 *   20. list.retainAll(collection) — keep only elements that exist in another collection
 *   21. list.containsAll(collection) — check if all elements of another collection exist
 *   22. list.iterator() — get an iterator
 *   23. list.stream() — convert to Stream
 *   24. Collections.reverse(list) — reverse the list
 *   25. Collections.frequency(list, val) — count occurrences of a value
 */
public class ArrayLists {
}
